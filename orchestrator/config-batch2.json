{
  "integration_branch": "integration/agent-pipeline-test",
  "max_concurrent": 15,
  "global_deadline_s": 5400,
  "pool_sids": [
    "a56c745b-ce11-4de1-98e6-e64d0d0cd4a3",
    "dcbc7306-d2cb-40e9-b6a6-8ddbda1784b9",
    "a78a8945-d732-4c3a-925f-d2f4f656a2b4",
    "1e4dd011-5134-49fd-8fd9-ad53d340cfaf",
    "592b8fe7-68a1-4a8b-86ad-a0c1c04f170b"
  ],
  "tasks": [
    {
      "id": "self-referencing-portal",
      "worker_model": "claude",
      "max_iters": 12,
      "worker_timeout_s": 300,
      "commit": "feat(qa): detector \u2014 portal whose destination is its own scene",
      "allowed_files": [
        "scripts/qa/self-referencing-portal.ts",
        "test/qa/self-referencing-portal.spec.ts"
      ],
      "verify_cmd": "npx vitest run test/qa/self-referencing-portal.spec.ts",
      "spec": "Create a deterministic detector: portal whose destination is its own scene. export function findSelfPortals(project): { offenders: Array<{scene:string}> } \u2014 portals whose destination scene id equals the scene they live in. Be defensive, never throw.\n\nFIRST read examples/game-engine/pharmacy-help-pt-a2/project.json and examples/game-engine/coffee-order-en-a1/project.json to learn the REAL shape and exact field names (scenes[].id, entities[].components[] kind/config, portals[], oralTaskGraphs[].nodes[], teacherActivities[]). Match the detector to the ACTUAL shape; if the concept is absent, implement generically and ensure BOTH example projects yield ZERO findings.\n\nHARD RULES: TypeScript only. NodeNext ESM, import the script as ../../scripts/qa/<name>.js (test is in test/qa/). Comments explain only WHY. No new deps. Surgical: create ONLY the two files. CLI `tsx scripts/qa/<name>.ts <project.json>` prints a JSON summary and process.exit(1) when findings non-empty. The vitest spec MUST (a) assert ZERO findings for both example projects and (b) include an inline fixture that triggers exactly one finding. VERIFY by running the exact verify command; if blocked, STOP and report.\n\nTest fixture: scene 'home' with a portal destination 'home' -> one offender."
    },
    {
      "id": "duplicate-portal-destinations",
      "worker_model": "claude",
      "max_iters": 12,
      "worker_timeout_s": 300,
      "commit": "feat(qa): detector \u2014 two portals in one scene point to the same destination",
      "allowed_files": [
        "scripts/qa/duplicate-portal-destinations.ts",
        "test/qa/duplicate-portal-destinations.spec.ts"
      ],
      "verify_cmd": "npx vitest run test/qa/duplicate-portal-destinations.spec.ts",
      "spec": "Create a deterministic detector: two portals in one scene point to the same destination. export function findDuplicatePortalDestinations(project): { offenders: Array<{scene:string;dest:string}> } \u2014 within a single scene, the same destination used by >1 portal. Be defensive, never throw.\n\nFIRST read examples/game-engine/pharmacy-help-pt-a2/project.json and examples/game-engine/coffee-order-en-a1/project.json to learn the REAL shape and exact field names (scenes[].id, entities[].components[] kind/config, portals[], oralTaskGraphs[].nodes[], teacherActivities[]). Match the detector to the ACTUAL shape; if the concept is absent, implement generically and ensure BOTH example projects yield ZERO findings.\n\nHARD RULES: TypeScript only. NodeNext ESM, import the script as ../../scripts/qa/<name>.js (test is in test/qa/). Comments explain only WHY. No new deps. Surgical: create ONLY the two files. CLI `tsx scripts/qa/<name>.ts <project.json>` prints a JSON summary and process.exit(1) when findings non-empty. The vitest spec MUST (a) assert ZERO findings for both example projects and (b) include an inline fixture that triggers exactly one finding. VERIFY by running the exact verify command; if blocked, STOP and report.\n\nTest fixture: scene 'home' with two portals both to 'shop' -> one offender (dest 'shop')."
    },
    {
      "id": "empty-oraltask-graph",
      "worker_model": "claude",
      "max_iters": 12,
      "worker_timeout_s": 300,
      "commit": "feat(qa): detector \u2014 oral task graph has no nodes",
      "allowed_files": [
        "scripts/qa/empty-oraltask-graph.ts",
        "test/qa/empty-oraltask-graph.spec.ts"
      ],
      "verify_cmd": "npx vitest run test/qa/empty-oraltask-graph.spec.ts",
      "spec": "Create a deterministic detector: oral task graph has no nodes. export function findEmptyOralTaskGraphs(project): { empty: string[] } \u2014 oralTaskGraphs whose nodes array is missing/empty (use the graph id or index). Be defensive, never throw.\n\nFIRST read examples/game-engine/pharmacy-help-pt-a2/project.json and examples/game-engine/coffee-order-en-a1/project.json to learn the REAL shape and exact field names (scenes[].id, entities[].components[] kind/config, portals[], oralTaskGraphs[].nodes[], teacherActivities[]). Match the detector to the ACTUAL shape; if the concept is absent, implement generically and ensure BOTH example projects yield ZERO findings.\n\nHARD RULES: TypeScript only. NodeNext ESM, import the script as ../../scripts/qa/<name>.js (test is in test/qa/). Comments explain only WHY. No new deps. Surgical: create ONLY the two files. CLI `tsx scripts/qa/<name>.ts <project.json>` prints a JSON summary and process.exit(1) when findings non-empty. The vitest spec MUST (a) assert ZERO findings for both example projects and (b) include an inline fixture that triggers exactly one finding. VERIFY by running the exact verify command; if blocked, STOP and report.\n\nTest fixture: an oralTaskGraph id 'g1' with nodes [] -> empty ['g1']."
    },
    {
      "id": "activity-without-oraltask",
      "worker_model": "claude",
      "max_iters": 12,
      "worker_timeout_s": 300,
      "commit": "feat(qa): detector \u2014 teacher activity not linked to any oral task graph",
      "allowed_files": [
        "scripts/qa/activity-without-oraltask.ts",
        "test/qa/activity-without-oraltask.spec.ts"
      ],
      "verify_cmd": "npx vitest run test/qa/activity-without-oraltask.spec.ts",
      "spec": "Create a deterministic detector: teacher activity not linked to any oral task graph. export function findActivitiesWithoutOralTask(project): { offenders: string[] } \u2014 teacherActivities that do not reference any existing oralTaskGraph (discover the linking field). Be defensive, never throw.\n\nFIRST read examples/game-engine/pharmacy-help-pt-a2/project.json and examples/game-engine/coffee-order-en-a1/project.json to learn the REAL shape and exact field names (scenes[].id, entities[].components[] kind/config, portals[], oralTaskGraphs[].nodes[], teacherActivities[]). Match the detector to the ACTUAL shape; if the concept is absent, implement generically and ensure BOTH example projects yield ZERO findings.\n\nHARD RULES: TypeScript only. NodeNext ESM, import the script as ../../scripts/qa/<name>.js (test is in test/qa/). Comments explain only WHY. No new deps. Surgical: create ONLY the two files. CLI `tsx scripts/qa/<name>.ts <project.json>` prints a JSON summary and process.exit(1) when findings non-empty. The vitest spec MUST (a) assert ZERO findings for both example projects and (b) include an inline fixture that triggers exactly one finding. VERIFY by running the exact verify command; if blocked, STOP and report.\n\nTest fixture: an activity 'a1' referencing no/unknown graph -> offenders ['a1']."
    }
  ],
  "golden_sid": "a56c745b-ce11-4de1-98e6-e64d0d0cd4a3"
}