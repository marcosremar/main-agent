# Orchestrator settings (loaded at startup by orchestrate.py)

# Model used by the CLAUDE worker lane for DOING tasks.
# The VALIDATOR is always Opus (claude-opus-4-8), regardless of this.
WORKER_CLAUDE_MODEL=claude-sonnet-4-6
